
- hosts: localhost
  roles:
    - dfe-azure

  vars_prompt:
    - name: ad_user
      prompt: Azure AD user
      default: "zoltan.kozma@cadenceinnova.com"
    - name: ad_password
      prompt: Azure AD password
      default: 'boo'
      private: yes
